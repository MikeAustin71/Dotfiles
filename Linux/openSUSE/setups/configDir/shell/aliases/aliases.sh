#!/bin/bash
# Mike's Aliases

alias aliases="bat ~/.config/shell/aliases/aliases.sh"

alias ip='ip -color'

# alias cat='bat -paging=never'

alias pnt='bat -paging=never'

alias pat='bat -paging=always'


alias cls="clear"

alias CLS="clear"

# alias cd="z"

alias tree="br"

alias top="btop"

# Uncomment this alias, only after
# trash-cli has been installed.
# alias rm="trash-put -iv"

alias update="sudo zypper update"

alias update2="sudo zypper dup"

alias update3="sudo zypper refresh && sudo zypper dup"
